# JSI
JSI. Java Shellcode Injector. It is Runner &amp; Payload Builder, is a lightweight red-team framework for **generating** and **executing** shellcode payloads using:  - **Donut** — converts EXE/DLL/.NET tools into position-independent shellcode   — executes payloads fully in memory.
